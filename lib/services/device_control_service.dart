class DeviceControlService {
  void turnOnDevice(String deviceId) {
    print('Turning on device $deviceId');
    // Logique pour allumer l'appareil
  }

  void turnOffDevice(String deviceId) {
    print('Turning off device $deviceId');
    // Logique pour éteindre l'appareil
  }

  void sendCommand(String deviceId, String command) {
    print('Sending command "$command" to device $deviceId');
    // Logique pour envoyer des commandes
  }
}
